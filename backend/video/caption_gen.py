def add_captions(video_path):
    """
    REAL VERSION:
    - Whisper: generate captions
    - FFmpeg: overlay animated captions

    CURRENT: bypass (returns original video)
    """

    return video_path
