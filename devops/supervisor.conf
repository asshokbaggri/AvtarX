[supervisord]
nodaemon=true

[program:backend]
command=uvicorn main:app --host 0.0.0.0 --port 8000
directory=/app
autostart=true
autorestart=true

[program:gpu_worker]
command=python3 gpu_worker.py
directory=/gpu
autostart=true
autorestart=true
